import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Campaign",
    description: "Campaign work by Fajar Kristiono.",
};

export default function CampaignPage() {
    return (
        <section className="page-placeholder">
            <h1>Campaign</h1>
            <p>Coming Soon</p>
        </section>
    );
}

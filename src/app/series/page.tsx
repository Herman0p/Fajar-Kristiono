import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Series",
    description: "Series by Fajar Kristiono.",
};

export default function SeriesPage() {
    return (
        <section className="page-placeholder">
            <h1>Series</h1>
            <p>Coming Soon</p>
        </section>
    );
}

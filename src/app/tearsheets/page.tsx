import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Tearsheets",
    description: "Tearsheets by Fajar Kristiono.",
};

export default function TearsheetsPage() {
    return (
        <section className="page-placeholder">
            <h1>Tearsheets</h1>
            <p>Coming Soon</p>
        </section>
    );
}

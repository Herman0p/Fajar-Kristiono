import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Tours",
    description: "Tours by Fajar Kristiono.",
};

export default function ToursPage() {
    return (
        <section className="page-placeholder">
            <h1>Tours</h1>
            <p>Coming Soon</p>
        </section>
    );
}
